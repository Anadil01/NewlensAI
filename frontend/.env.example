# Base URL of the NewsLensAI backend, including the /api prefix.
# The backend defaults to PORT=5001 (see backend/.env.example).
# Vite only reads this file at server start, so restart `npm run dev` after changing it.
VITE_API_URL=http://localhost:5001/api
